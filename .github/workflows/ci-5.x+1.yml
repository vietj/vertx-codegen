name: vertx-codegen (5.x+1)
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    - cron: '*/10 * * * *'
jobs:
  CI_CD:
    uses: ./.github/workflows/ci∕cd-5.x.yml
    secrets: inherit
