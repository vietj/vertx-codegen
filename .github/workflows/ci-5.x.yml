name: vertx-codegen (5.x)
run-name: vertx-codegen (${{ github.ref_name }})
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    - cron: '0 5 * * *'
jobs:
  CI_CD:
    uses: ./.github/workflows/ci∕cd-5.x.yml
    secrets: inherit
