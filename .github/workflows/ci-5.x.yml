name: vertx-codegen (${{ github.ref_name }})
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    - cron: '0 5 * * *'
jobs:
  CI_CD:
    uses: ./.github/workflows/cicd-5.x.yml
    secrets: inherit
