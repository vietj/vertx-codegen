name: vertx-codegen (5.x)
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    - cron: '0 5 * * *'
jobs:
  CI_Deploy:
    uses: ./.github/workflows/ci_deploy-5.x.yml
    secrets: inherit
